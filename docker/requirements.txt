# Direct app deps for whisper-fw.
# CTranslate2, torch, whisperx are installed in the Dockerfile (torch from
# the cu130 index, CTranslate2 from source with CUDA, whisperx --no-deps).
# This file installs the remaining direct deps.
# Full 142-package freeze in requirements-full-freeze.txt for bit-exact repro.

faster-whisper==1.2.1
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32